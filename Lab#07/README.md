# SQL Filters — Pattern Matching Only

## Scope
This submission is based on the uploaded LAB_06 SQL Filters manual. The manual defines pattern matching with `LIKE` and the `%` and `_` wildcards. fileciteturn4file0L198-L216

Only the pattern-matching tasks are included.

## Employee Tasks
- B6 — names starting with `M`
- B7 — names containing `a`
- B8 — names ending with `an`
- B9 — job titles containing `Engineer` while not being in the Engineering department

These tasks are listed in the manual. fileciteturn4file0L293-L305

## Bookstore Assessment
- Q4 — titles containing `the`
- Q5 — titles starting with `A` or ending with `t`

These are the pattern-matching assessment questions. fileciteturn4file0L369-L381

## Excluded
All other Employee and Bookstore tasks are intentionally excluded. This submission does not perform comparison, logical, BETWEEN, IN, NULL, ORDER BY, or LIMIT tasks.

## Files
- `RollNo_Pattern_Matching_Only.sql`
- `README_Pattern_Matching_Only.md`

## Running
Open the SQL file in MySQL Workbench/MySQL 8.x and run it. The script creates both databases, creates the required tables, loads the manual's sample data, and runs only the requested pattern-matching queries.

The Employee table structure/sample data comes from the manual. fileciteturn4file0L46-L72
The Book table structure/sample data comes from the bookstore assessment. fileciteturn4file0L317-L366
